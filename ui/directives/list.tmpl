<script type="text/ng-template" id="infoBox.html">
    <div class="modal-header">
        <h3 class="modal-title">{{title}}</h3>
    </div>

    <div ng-bind-html=pretty style= "padding-left:20px;"  class="preBox"></div>

<!-- data debug
    <div class="modal-body">
        <pre id="reg" class="preBox" scroll-glue><code class="javascript">{{oneDataRecord}}</code></pre>
    </div>
    -->
    <div class="modal-footer">
        <button class="btn btn-primary" ng-click="ok()">OK</button>
    </div>
</script>
<section ng-controller="shoppingCartCtrl">
    <div>
        <!-- if scope.access.add is allowed, show button-->
        <input type="button" value="Add New Cart" ng-click="addCart();" ng-if="access.addCart" class="btn btn-primary btn-sm"/>
        <!-- UI Grid directive -->
        <nglist></nglist>
    </div>
</section>
<style>
.title{
	font-size: 16px;
}

.data{
	font-size:14px;
	color:grey;
	margin-left: 12px;
}
.fieldset {
    display: block;
    margin-left: 2px;
    margin-right: 2px;
    padding-top: 0.35em;
    padding-bottom: 0.625em;
    padding-left: 0.75em;
    padding-right: 0.75em;
    border: 2px  grey dashed;
    margin-bottom: 12px;
    margin-top: 2px;
}
.legend {
font-size:15px;
font-weight:bold;
    display: block;
    padding-left: 2px;
    padding-right: 2px;
    border: none;
}
.shippingMethods , .filters{
	margin-left: 7px;
}
.shippingMethods .data ,  .shippingMethods .title
.filters .data ,  .filters .title{
	font-size: 12px;
	font-style: italic;
}
</style>